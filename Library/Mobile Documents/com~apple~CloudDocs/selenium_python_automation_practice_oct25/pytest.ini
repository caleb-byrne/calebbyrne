[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py *_test.py TS_*.pytest ts_*.py
python_functions = test_* tc_*
addopts = -q -r a

# Example markers (uncomment and add to tests if you use them)
# markers =
#     smoke: quick smoke tests
#     slow: slower tests that require network or browser
